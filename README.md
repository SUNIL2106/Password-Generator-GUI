# Password-Generator-GUI
Developed a modern desktop GUI application using Python and CustomTkinter that generates secure passwords through multiple modes, including simple, complex, and fully customized passwords. The application allows users to specify password length, generate random secure passwords, and save them locally through an intuitive graphical user interface.
